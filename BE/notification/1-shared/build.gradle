apply plugin: 'org.springframework.boot'

dependencies {
	// No additional dependencies needed for DTOs
}

// Shared module is a library, not a bootable application
bootJar {
	enabled = false
}

jar {
	enabled = true
}
